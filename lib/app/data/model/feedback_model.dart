import 'package:flutter/cupertino.dart';

class FeedbackModel {
  final String ? name ;
  final String ? feedback ;
  FeedbackModel(
  {
    @required this.name,
    @required this.feedback, required int id,
}
      );
}